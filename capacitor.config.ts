import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gonzalo.anima.herramientas.combate',
  appName: 'Herramientas de Combate',
  webDir: 'dist'
};

export default config;
